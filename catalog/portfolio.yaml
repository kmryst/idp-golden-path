# ポートフォリオ全体を横断する System 定義。
#
# terraform-hannibal / ticket-c2c-platform の Component 定義は
# 各リポジトリ自身が所有する catalog-info.yaml に移行済み。
# backstage/app-config.yaml / app-config.production.yaml の catalog.locations で
# GitHub URL location として取り込む（ADR-0011 参照）。
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: kmryst-portfolio
  description: DevOps / SRE / Platform Engineering ポートフォリオを構成するリポジトリ群
spec:
  owner: platform
